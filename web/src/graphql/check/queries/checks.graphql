query Checks($patientId: String) {
  checks(patientId: $patientId) {
    checks {
      id
      localisation
      variant
      confidence
      scanDate
      scan
    }
    errors {
      field
      message
    }
  }
}
