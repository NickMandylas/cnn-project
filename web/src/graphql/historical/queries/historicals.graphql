query Historicals($patientId: String!) {
  historicals(patientId: $patientId) {
    historicals {
      id
      variant
      scan
      scanDate
    }
    errors {
      field
      message
    }
  }
}
